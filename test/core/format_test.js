import assert from 'assert';
import { describe, it } from 'mocha';

describe('format', () => {
  describe('#render()', () => {
      it('date', function() {
          console.log("...")
      });
  });
});
